---
name: Feature request
about: Propose a new directive or capability (spec-first)
title: "feat: "
labels: ["enhancement"]
assignees: chulingera2025
---

## Problem

<!-- What capability is missing and why? What production pain does it solve?
     Reference the ROADMAP "待开发优化功能" queue if it is already listed. -->

## Proposed behavior

<!-- For a new Raddexfile directive: the proposed syntax and semantics.
     NOTE the spec-first red line: `docs/RADDEXFILE_SPEC.md` is the source of
     truth and MUST be updated (and reviewed) before the syntax is implemented.
     Link the spec section this would extend (e.g. §4 trusted_proxies, §8 Todo). -->

**Proposed syntax**
```caddyfile

```

## Design considerations

<!-- Anything that needs a decision first — e.g. JWKS discovery vs static keys
     for `jwt`, IP trust model for `trusted_proxies`. Should this be recorded as
     an ADR in devdoc/DECISIONS.md before implementation? -->

## Acceptance criteria

<!-- Concrete, verifiable outcomes (model these on the ROADMAP milestone
     checklists). Include fuzz coverage if the parser grammar changes. -->

- [ ]
- [ ]

## Related

<!-- Link the ROADMAP milestone and/or RADDEXFILE_SPEC section. -->
