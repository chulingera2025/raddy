ARG CADDY_BUILDER_IMAGE
ARG CADDY_RUNTIME_IMAGE
ARG CADDY_VERSION
ARG CADDY_L4_VERSION
FROM ${CADDY_BUILDER_IMAGE} AS builder

ARG CADDY_VERSION
ARG CADDY_L4_VERSION
RUN xcaddy build "v${CADDY_VERSION}" --with "github.com/mholt/caddy-l4@${CADDY_L4_VERSION}"

FROM ${CADDY_RUNTIME_IMAGE}

COPY --from=builder /usr/bin/caddy /usr/bin/caddy
